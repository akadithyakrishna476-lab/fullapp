# 🔄 Timetable System - Data Flow & Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        COLLEGE TIMETABLE SYSTEM                      │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│   Faculty App    │         │   Firebase       │         │   CR App         │
│   (Edit Mode)    │◄───────►│   Firestore      │◄───────►│   (View Only)    │
└──────────────────┘         └──────────────────┘         └──────────────────┘
        │                             │                             │
        │ 1. Add/Edit                 │ 2. Real-time               │ 3. Auto-update
        │    Timetable                │    Sync                    │    Display
        ▼                             ▼                             ▼
   [WRITE access]              [Data Storage]              [READ access]
```

---

## Data Flow Diagram

### 1. Faculty Adds/Edits Timetable

```
┌────────────┐
│  Faculty   │
│  Login     │
└─────┬──────┘
      │
      ▼
┌─────────────────────┐
│ Load Faculty Data   │
│ • Get UID           │
│ • Get Classes Array │ ──────┐
└─────┬───────────────┘       │
      │                        │ Firebase Query
      ▼                        │ /faculty/{uid}
┌─────────────────────┐       │
│ Display Timetable   │       │
│ Framework (Mon-Fri, │◄──────┘
│ 6 Periods)          │
└─────┬───────────────┘
      │
      ▼
┌─────────────────────┐
│ Click "Edit         │
│ Timetable" Button   │
└─────┬───────────────┘
      │
      ▼
┌─────────────────────┐
│ Enter Edit Mode     │
│ • Enable cell edits │
│ • Show Save/Cancel  │
└─────┬───────────────┘
      │
      ▼
┌─────────────────────┐
│ Faculty Edits Cells │
│ • Tap cell          │
│ • Enter subject     │
│ • Enter class       │
│ • Validate          │
└─────┬───────────────┘
      │
      ▼
┌─────────────────────┐
│ Click "Save         │
│ Changes"            │
└─────┬───────────────┘
      │
      ▼
┌─────────────────────────────────┐
│ Group by Class & Save to        │
│ Firebase                         │
│                                  │
│ For each class:                 │
│   timetables/{classSection}     │
│   ├─ class: "CS-A"              │
│   ├─ lastUpdated: timestamp     │
│   ├─ updatedBy: facultyUID      │
│   └─ schedule: {                │
│       "Monday-Period1": {...}   │
│       "Tuesday-Period3": {...}  │
│     }                            │
└─────┬───────────────────────────┘
      │
      ▼
┌─────────────────────┐
│ Success Message     │
│ Exit Edit Mode      │
└─────────────────────┘
```

### 2. CR Views Timetable (Real-time Updates)

```
┌────────────┐
│    CR      │
│   Login    │
└─────┬──────┘
      │
      ▼
┌─────────────────────┐
│ Load CR Data from   │
│ AsyncStorage        │
│ • Get class field   │ ──────┐
└─────┬───────────────┘       │
      │                        │
      ▼                        │
┌─────────────────────┐       │
│ Set Up Real-time    │       │ Subscribe to
│ Firebase Listener   │       │ /timetables/{class}
│ onSnapshot()        │◄──────┘
└─────┬───────────────┘
      │
      ▼
┌─────────────────────────────────┐
│ Display Timetable               │
│ • Show Mon-Fri framework        │
│ • Display 6 periods             │
│ • Populate with class data      │
│ • Read-only view                │
└─────┬───────────────────────────┘
      │
      │ ┌───────────────────────────┐
      │ │  FACULTY SAVES CHANGES    │
      │ │  (From Faculty App)       │
      │ └───────┬───────────────────┘
      │         │
      │         ▼
      │ ┌───────────────────────────┐
      │ │  Firebase Document        │
      │ │  Updated                  │
      │ └───────┬───────────────────┘
      │         │
      │         │ Real-time Notification
      │         │
      ▼         ▼
┌─────────────────────────────────┐
│ CR's Timetable AUTO-UPDATES     │
│ • No refresh needed             │
│ • Instant update (1-2 sec)      │
│ • Only for CR's class           │
└─────────────────────────────────┘
```

---

## Component Interaction Flow

```
┌────────────────────────────────────────────────────────────────┐
│                     TimetableScreen.js                          │
└────────────────────────────────────────────────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐   ┌──────────────────┐   ┌───────────────┐
│ Role Detection│   │  Data Loading    │   │  UI Rendering │
├───────────────┤   ├──────────────────┤   ├───────────────┤
│ • Check auth  │   │ • Faculty: load  │   │ • Week view   │
│ • Faculty?    │   │   all classes    │   │ • Daily view  │
│ • CR?         │   │ • CR: subscribe  │   │ • Edit modal  │
│ • Load data   │   │   to class doc   │   │ • Buttons     │
└───────┬───────┘   └────────┬─────────┘   └───────┬───────┘
        │                    │                      │
        └────────────────────┼──────────────────────┘
                             │
                             ▼
                  ┌────────────────────┐
                  │  State Management  │
                  ├────────────────────┤
                  │ • timetable        │
                  │ • isEditMode       │
                  │ • userRole         │
                  │ • loading          │
                  └────────────────────┘
```

---

## Firebase Data Structure

```
Firestore Database
│
├─ faculty/
│  ├─ {facultyUID1}/
│  │  ├─ name: "Dr. John"
│  │  ├─ email: "john@college.edu"
│  │  ├─ classes: ["CS-A", "CS-B", "ME-A"]  ← Key for permissions
│  │  └─ department: "Computer Science"
│  │
│  └─ {facultyUID2}/
│     └─ ...
│
├─ timetables/
│  ├─ CS-A/  ← Document ID = Class Section
│  │  ├─ class: "CS-A"
│  │  ├─ lastUpdated: "2026-01-19T10:30:00Z"
│  │  ├─ updatedBy: "facultyUID1"
│  │  └─ schedule: {
│  │      "Monday-Period1": {
│  │          subject: "Data Structures",
│  │          class: "CS-A",
│  │          day: "Monday",
│  │          period: 1,
│  │          time: "09:00 - 10:00"
│  │      },
│  │      "Monday-Period2": { ... },
│  │      "Tuesday-Period1": { ... },
│  │      ...
│  │  }
│  │
│  ├─ CS-B/
│  │  └─ ...
│  │
│  └─ ME-A/
│     └─ ...
│
└─ classRepresentatives/ (optional)
   ├─ {crUID1}/
   │  ├─ name: "Jane Smith"
   │  ├─ class: "CS-A"  ← Key for real-time listener
   │  └─ email: "jane@college.edu"
   │
   └─ {crUID2}/
      └─ ...
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    State Variables                           │
└─────────────────────────────────────────────────────────────┘
        │
        ├─► timetable: {}               ← Current timetable data
        ├─► originalTimetable: {}       ← Backup for cancel
        ├─► isEditMode: false           ← Faculty edit state
        ├─► userRole: 'faculty'|'cr'    ← User type
        ├─► facultyClasses: []          ← Classes faculty handles
        ├─► userClass: 'CS-A'           ← CR's class
        ├─► loading: true               ← Loading state
        ├─► showEditModal: false        ← Modal visibility
        ├─► selectedSlot: {}            ← Current editing slot
        └─► viewMode: 'week'|'daily'    ← Display mode

┌─────────────────────────────────────────────────────────────┐
│                    State Updates                             │
└─────────────────────────────────────────────────────────────┘

Faculty Edit Flow:
1. Click "Edit" → isEditMode = true
2. Edit cells → timetable updates (local)
3. Click "Save" → Firebase write → Success
4. Click "Cancel" → timetable = originalTimetable

CR View Flow:
1. Load → Set up onSnapshot listener
2. Firebase update → Listener triggers
3. Callback → setTimetable(newData)
4. UI auto-refreshes
```

---

## Real-time Update Mechanism

```
┌──────────────────────────────────────────────────────────────┐
│              Firebase onSnapshot Listener                     │
└──────────────────────────────────────────────────────────────┘

Initial Setup (CR App Load):
┌─────────────────────────────────────────────────────┐
│ const timetableRef = doc(db, 'timetables', classSection); │
│                                                      │
│ const unsubscribe = onSnapshot(timetableRef, (doc) => {    │
│   if (doc.exists()) {                              │
│     const data = doc.data();                       │
│     setTimetable(data.schedule);  ← Updates state  │
│   }                                                 │
│ });                                                 │
└─────────────────────────────────────────────────────┘

When Faculty Saves:
┌─────────────────────────────────────────────────────┐
│ Faculty App                                         │
│   ↓                                                 │
│ setDoc(timetableRef, {...})  ← Write to Firebase   │
│   ↓                                                 │
│ Firebase detects change                             │
│   ↓                                                 │
│ onSnapshot callback triggered on ALL listeners      │
│   ↓                                                 │
│ CR's callback executes                              │
│   ↓                                                 │
│ setTimetable(newData)  ← CR state updates           │
│   ↓                                                 │
│ React re-renders UI                                 │
│   ↓                                                 │
│ CR sees updated timetable (1-2 seconds)             │
└─────────────────────────────────────────────────────┘
```

---

## Edit Mode vs View Mode

```
┌────────────────────────────────────────────────────────────┐
│                    View Mode (Default)                      │
└────────────────────────────────────────────────────────────┘
│
├─ Faculty:
│  • See all timetables for their classes
│  • "Edit Timetable" button visible
│  • Cells are NOT clickable
│  • Can toggle Week/Daily view
│
└─ CR:
   • See timetable for their class only
   • No edit button
   • Read-only
   • Real-time updates active

┌────────────────────────────────────────────────────────────┐
│                    Edit Mode (Faculty Only)                 │
└────────────────────────────────────────────────────────────┘
│
├─ UI Changes:
│  • "Edit Timetable" → "Cancel" & "Save Changes" buttons
│  • All cells get blue border (editable indicator)
│  • Info text updates
│  • Empty cells show "+" icon
│
├─ Interactions:
│  • Tap any cell → Opens edit modal
│  • Fill subject & class → Save
│  • Existing class → Edit or Delete
│  • Cancel → Discard all changes
│  • Save → Write to Firebase
│
└─ Validation:
   • Subject required
   • Class required
   • Class must be in facultyClasses array
```

---

## Security Layer

```
┌────────────────────────────────────────────────────────────┐
│                 Firebase Security Rules                     │
└────────────────────────────────────────────────────────────┘

Client Request:
┌──────────────┐
│  Faculty App │
│  Writes to:  │
│  /timetables │
│  /CS-A       │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Firebase Security Rules Engine      │
│                                       │
│  1. Is user authenticated? ✓         │
│  2. Is user in faculty collection? ✓ │
│  3. Does faculty handle CS-A? ✓      │
│  4. Is data structure valid? ✓       │
│                                       │
│  → ALLOW WRITE                        │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────┐
│  Firestore   │
│  Data Saved  │
└──────────────┘

Blocked Request Example:
┌──────────────┐
│  CR App      │
│  Tries to:   │
│  Write to    │
│  /timetables │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Firebase Security Rules Engine      │
│                                       │
│  1. Is user authenticated? ✓         │
│  2. Is user in faculty collection? ✗ │
│                                       │
│  → DENY WRITE                         │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────┐
│  Permission  │
│  Denied      │
│  Error       │
└──────────────┘
```

---

## Performance Optimization

```
┌────────────────────────────────────────────────────────────┐
│              Performance Strategies                         │
└────────────────────────────────────────────────────────────┘

1. Lazy Loading:
   • Load only relevant class data
   • Faculty: Load only their classes
   • CR: Load only their class

2. Real-time Efficiency:
   • onSnapshot only for CR (read-heavy)
   • Faculty uses one-time reads and writes
   • Unsubscribe listeners on unmount

3. Local State Management:
   • Edit mode changes stored locally
   • Batch write to Firebase on save
   • Reduce Firebase read/write operations

4. UI Optimization:
   • FlatList for Daily view (virtualization)
   • Conditional rendering based on viewMode
   • Memoization for expensive calculations

5. Data Structure:
   • Flat schedule object (fast lookups)
   • Key format: "Monday-Period1" (predictable)
   • Indexed by class section (efficient queries)
```

---

**Document Version**: 1.0  
**Last Updated**: January 19, 2026  
**Maintainer**: Development Team
